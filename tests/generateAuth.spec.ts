// tests/generateAuth.spec.ts
// Test helper cuyo único propósito es disparar `global-setup.ts` para generar
// `test-artifacts/auth.json`. No realiza acciones por sí mismo.
import { test } from '@playwright/test';

test('generate auth state (helper)', async ({}) => {});
